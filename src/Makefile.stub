#
# This is the stub Makefile. If you did run ./configure and have GNU make,
# you are probably more interested in looking at GNUmakefile.
#
generic: default

help: default

all: default

install: default

default:
	@echo "For listing legacy builds, use \"make -f Makefile.legacy\"."
	@echo "For using legacy Makefile, use \"make -sf Makefile.legacy <target>\"."
	@echo
	@echo "A normal build is now \"./configure && make -s\"."
	@echo
	@exit 1

# Unfortunately these does not work due to the @EXE_EXT@ shite
#clean:
#	$(MAKE) -f Makefile.in $@
#
#distclean:
#	$(MAKE) -f Makefile.in $@

# Better than nothing :-/
clean:
	+$(MAKE) -f Makefile.legacy $@

distclean: clean
